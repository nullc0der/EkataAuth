{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run Auth Django Server",
            "type": "shell",
            "command": "pipenv run python manage.py runserver 8001"
        },
        {
            "label": "Run Auth Celery",
            "type": "shell",
            "command": "pipenv run celery -A ekataauthserver worker -l info "
        }
    ]
}
